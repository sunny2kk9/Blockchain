# Move sample package

This folder contains a minimal Move package `hello_move` with a single module `Hello`.

Files:
- `Move.toml` - package manifest
- `sources/hello.move` - Move module source

Deployment (Aptos devnet) instructions are in the repository root `deploy_instructions.md`.
